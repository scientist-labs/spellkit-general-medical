# For available configuration options, see:
#   https://github.com/standardrb/standard
ruby_version: 3.1

# bin/ scripts carry no .rb extension, so standard would not find them on its own.
include:
  - "bin/fetch_chv"
  - "bin/validate"
